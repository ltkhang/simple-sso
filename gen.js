const crypto = require('crypto');

console.log(Buffer.from(crypto.randomBytes(32)).toString('hex'));
console.log(Buffer.from(crypto.randomBytes(32)).toString('hex'));
console.log(Buffer.from(crypto.randomBytes(32)).toString('hex'));
console.log(Buffer.from(crypto.randomBytes(32)).toString('hex'));